{
  "$comment": "Type-checks and compiles bedside-mode.ts as a browser script — DOM lib, no Node types, no framework. Separate from the backend's own tsconfig.json (which targets Node, has no DOM lib, and is not in strict mode) because this file ships to the browser and deserves stricter guarantees than server code. Run via `npm run bedside:build`.",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM"],
    "module": "None",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "noEmitOnError": true,
    "outDir": "./out",
    "types": []
  },
  "include": ["bedside-mode.ts"]
}
